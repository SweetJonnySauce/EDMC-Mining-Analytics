# Context — Reconcile SRV Cargo

The supplied log shows `MiningRefined` followed by SRV `Cargo` entries that expose only a `Count`, not `Inventory`. Per explicit user direction, treat that count as the total across all SRV commodities.
